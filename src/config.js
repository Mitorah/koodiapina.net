// Application configuration

export const config = {
  // Message notification settings
  message: {
    duration: 4 * 1000, // Duration for notification messages (4 seconds)
  },
  
  // Version check settings
  version: {
    checkInterval: 15 * 60 * 1000, // How often to check for new versions (15 minutes)
  },
  
  // Add more configuration sections as needed
};
